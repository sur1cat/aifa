apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "habit-service.fullname" . }}
  labels: {{- include "habit-service.labels" . | nindent 4 }}
data:
  PORT: {{ .Values.service.targetPort | quote }}
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
